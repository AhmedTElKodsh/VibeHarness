# Unresolved Questions

- Should `schemas/*.json` become executable validation sources, or remain documentation mirrors of `src/validation.ts`?
- What exact `adapter-task.yaml` fields should OpenCode receive when P1 starts?
- Which OpenHands-only fields, if any, belong in namespaced adapter config rather than core contracts?
- Should `vibeharness explain` remain in the backlog or be removed from the P0 planning bundle?
- Which generated docs should be considered source artifacts versus disposable run output?
